// JavaScript Documentvar dom = document.getElementById ? true:false;var nn4 = document.layers ? true:false;var ie4 = document.all ? true:false;if (dom) {ie4=false;} /* IE5 should use DOM */lastOne = false;/*image = new Array();for (var i=1; i<=10; i++) {	image[i] = new Image();	image[i].src = pic[i];}*/function stickIt(which) {	 if (which) {		cur = which;	 } else if (location.search!="") {		cur = location.search.substr(1);	 } else {		cur = 1;	 }	 if (dom) {		 //alert(pic[cur]);		 //fix for IE weirdness		 document.getElementById('mainImage').src = "images/rule.gif";		 //now back to the good stuff		 document.getElementById('mainImage').src = pic[cur];		 document.getElementById('mainImage').height = hig[cur];		 document.getElementById('mainImage').width = wid[cur];	 } else {		 document.mainImage.src = "images/rule.gif";		 document.mainImage.src = pic[cur];		 document.mainImage.height = hig[cur];		 document.mainImage.width = wid[cur];	 }	 if (lastOne) {		lastPic = eval("document.pic"+lastOne);		//lastPic.border = 0;		//lastPic.removeAttribute(className);	 }	 thisPic = eval("document.pic"+cur);	 //thisPic.border = 1;	 //thisPic.className = "bordered";	 //thisPic.blur();	 lastOne = cur;	 document.getElementById("caption").innerHTML = cap[cur];}